{
  "name": "custom-react-pdf-viewer-monorepo",
  "private": true,
  "scripts": {
    "demo": "pnpm --filter demo dev",
    "build:lib": "pnpm --filter custom-react-pdf-viewer build",
    "publish:npm": "pnpm build:lib && pnpm --filter custom-react-pdf-viewer publish --no-git-checks"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "pnpm": "^8.0.0",
    "tsup": "^8.0.2",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "vite-tsconfig-paths": "^5.1.4"
  }
}